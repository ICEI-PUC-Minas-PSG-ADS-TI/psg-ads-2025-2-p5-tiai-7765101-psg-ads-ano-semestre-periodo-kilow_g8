import styled from 'styled-components';
import { Tooltip } from '@base-ui/react/tooltip';

export const StyledTooltipIcon = styled(Tooltip.Trigger)`
  border: none;
  background: transparent;
  padding: 0;
  cursor: pointer;
  align-self: center;
  display: flex;
`;
